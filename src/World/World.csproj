<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
    <RootNamespace>UNNAMED.World</RootNamespace>
    <AssemblyName>UNNAMED.World</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Domain\Domain.csproj" />
    <ProjectReference Include="..\EntityRegistry\EntityRegistry.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- World mutation is internal (ARCHITECTURE.md §5); tests and the M2 probe build fixture worlds. -->
    <InternalsVisibleTo Include="World.Tests" />
    <InternalsVisibleTo Include="Persistence.Tests" />
    <InternalsVisibleTo Include="M2.Probe" />
  </ItemGroup>

</Project>
